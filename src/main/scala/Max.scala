class Max {
  /*def FindMax(list: List[Int]): Int={
  val list:List[Int]= List(2,3,4,19)
  var max=list(0)
  //for(current<-0 to list.length-1)
  /*  {
      if(list(current)>max)
        max=list(current)
     var a= list.foreach(
        println(a)
      )
   */ var a=list.foreach(
    println(a)
  )
}
/
 */

}
